  
  点 
	Point:
		constructor:(x, y)
			api
				draw(context)
				like(point) //是否与给入点位置相近
				setX(x)
				setY(y)
			event
				onclick
				onmouseover
				onmouseout
		
	Line:
		constructor:(point, point)
	
	Rect:
		constructor:(point, width, height)
		constructor:(point, point, point)
		
	
		
	