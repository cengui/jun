
/**
  
  JUNE
    //-- class
    .Vector2(x, y),
        .setXY(x, y);
        .length();
		.addSelf( v );
		.subSelf( v );
		.multiplyScalar( s )
		.clone();
		
    .Vector3(x, y, z),
		.setXYZ(x, y, z)
		.addSelf();
    .Stage(canvas, width, height);
    
        .addChild( child );
        .removeChild( child );
        .removeChindIndex( index );
    
    //--interface
    .Event
        addEvent(type, handle)
        removeEvent(type, headle)
        
*/


June = {};